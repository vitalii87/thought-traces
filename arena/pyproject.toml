[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "iah-arena"
version = "0.1.0"
description = "Provider-neutral laboratory for Intelligence Attractor experiments"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
